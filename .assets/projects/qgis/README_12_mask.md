# 蒙版图层生成工具 - 使用说明

## 📌 功能说明

创建差异化透明度的白色蒙版，实现区域内外不同的视觉效果。适合地图可视化中突出显示特定区域。

## 🎯 效果示例

**当 OUTER_LAYER = "all" 时（推荐）：**

```
┌────────────────────────────────────────────┐
│  整个地图范围 (白色10%透明 - 几乎不透明) │
│                                            │
│    ┌───────────────────────┐               │
│    │  Jingning             │               │
│    │  (白色50%透明)        │               │
│    │  ← 这里更透明         │               │
│    │     底图更清晰        │               │
│    └───────────────────────┘               │
│                                            │
│  ← 地图其他所有区域都被白色蒙版遮挡       │
│                                            │
└────────────────────────────────────────────┘
```

**当 OUTER_LAYER = "county" 时：**

```
┌─────────────────────────────────────────┐
│  (county范围外无蒙版)                   │
│  ┌───────────────────────────────────┐  │
│  │ County (白色10%透明)              │  │
│  │    ┌───────────────────────┐      │  │
│  │    │  Jingning (白色50%)   │      │  │
│  │    └───────────────────────┘      │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

**视觉效果**：
- ✨ Jingning区域：透明度高，底图清晰可见（关注区域）
- 🔲 其他区域：透明度低，底图被遮挡（背景区域）
- 💡 使用 "all" 可以覆盖整个地图，不受图层范围限制

## 🚀 快速使用

### 1. 在QGIS中加载图层

确保你已经加载了：
- `jingning` - 内部区域（景宁县）
- **可选**：`county` - 外部区域（如果想限制在特定范围）
- **推荐**：使用 `OUTER_LAYER = "all"` - 自动使用整个地图画布范围

### 2. （可选）修改配置

打开 `12_create_mask_layers.py`，可以修改：

```python
# 图层名称
INNER_LAYER = "jingning"        # 内部区域（你的实际图层名）
OUTER_LAYER = "all"             # 外部区域设置：
                                # "all" = 整个地图画布范围（推荐！）
                                # 或指定具体图层名如 "county"

# 透明度设置 (0-100)
INNER_OPACITY = 50              # 内部区域透明度（默认50%）
OUTER_OPACITY = 10              # 外部区域透明度（默认10%）

# 蒙版颜色
MASK_COLOR = (255, 255, 255)    # 白色，也可以改为黑色 (0, 0, 0)
```

**💡 关于 OUTER_LAYER 的选择：**

- **`OUTER_LAYER = "all"`**（推荐）
  - ✅ 覆盖整个地图画布，不受任何图层范围限制
  - ✅ 效果最好，jingning 以外的所有区域都被蒙版覆盖
  - ✅ 不需要额外加载图层
  
- **`OUTER_LAYER = "county"`**（指定图层）
  - ⚠️  只覆盖 county 图层的范围
  - ⚠️  如果 county 范围较小，县外区域不会被蒙版
  - ✅ 适合需要精确控制蒙版范围的情况

### 3. 运行脚本

**方法1：在QGIS Python控制台（推荐）**

```python
exec(open('~/useful_scripts/.assets/projects/qgis/12_create_mask_layers.py').read())
```

**方法2：从终端运行**

```bash
cd ~/useful_scripts/.assets/projects/qgis
./run_12.sh
```

### 4. 查看结果

生成的图层会自动出现在 **mask_layers** 组中：
- `jingning_mask` - 透明度50%
- `county_excluding_jingning_mask` - 透明度10%

**重要**：确保你的底图（如卫星影像、地形图等）在蒙版图层**下方**，才能看到蒙版效果。

## ⚙️ 配置参数详解

### 透明度设置

```python
INNER_OPACITY = 50   # 0-100，数值越大越不透明
OUTER_OPACITY = 10   # 0-100，数值越大越不透明
```

**常见配置**：

| 场景 | 内部透明度 | 外部透明度 | 效果 |
|------|-----------|-----------|------|
| **突出内部**（默认） | 50 | 10 | 内部清晰，外部模糊 |
| 强烈对比 | 70 | 5 | 内部非常清晰，外部几乎白色 |
| 温和对比 | 40 | 20 | 对比柔和 |
| 反向突出 | 10 | 50 | 外部清晰，内部模糊 |

### 颜色设置

```python
MASK_COLOR = (255, 255, 255)  # RGB格式
```

**常用颜色**：
- 白色：`(255, 255, 255)` - 默认，适合深色底图
- 黑色：`(0, 0, 0)` - 适合浅色底图
- 灰色：`(128, 128, 128)` - 中性色调
- 蓝色：`(173, 216, 230)` - 淡蓝色蒙版

## 💡 实际应用场景

### 场景1：研究区域展示

```python
INNER_LAYER = "study_area"      # 研究区域
OUTER_LAYER = "province"        # 所在省份
INNER_OPACITY = 60              # 研究区清晰
OUTER_OPACITY = 10              # 省份其他区域模糊
```

**用途**：论文、报告中突出研究区域位置

### 场景2：多层级行政区

```python
INNER_LAYER = "downtown"        # 市中心
OUTER_LAYER = "city"            # 整个城市
INNER_OPACITY = 50
OUTER_OPACITY = 20
```

**用途**：城市规划、人口分布可视化

### 场景3：保护区规划

```python
INNER_LAYER = "core_zone"       # 核心保护区
OUTER_LAYER = "buffer_zone"     # 缓冲区
INNER_OPACITY = 70              # 核心区更醒目
OUTER_OPACITY = 30
```

## 🎨 图层调整技巧

### 调整透明度（运行后）

1. 右键图层 → **属性**
2. 选择 **符号系统**
3. 点击 **简单填充**
4. 调整 **不透明度** 滑块

### 调整颜色（运行后）

1. 右键图层 → **属性**
2. 选择 **符号系统**
3. 点击 **填充颜色**
4. 选择新颜色

### 图层顺序

为了正确显示蒙版效果，图层顺序应该是：

```
(上)
├─ mask_layers 组
│  ├─ jingning_mask              ← 最上层
│  └─ county_excluding_jingning_mask
├─ 底图（卫星影像、地形图等）    ← 中间
└─ 原始数据图层                  ← 最下层
(下)
```

## 🔧 故障排除

### 问题1：看不到蒙版效果

**原因**：底图在蒙版上方

**解决**：在图层面板中，将底图拖到蒙版图层下方

### 问题2：蒙版完全不透明

**原因**：透明度设置为100或接近100

**解决**：降低 `OUTER_OPACITY` 和 `INNER_OPACITY` 的值

### 问题3：找不到输出图层

**原因**：输出图层在 mask_layers 组中

**解决**：在图层面板中展开 mask_layers 组

### 问题4：外部蒙版没有扣除内部

**原因**：空间差集运算可能失败

**解决**：
1. 检查两个图层是否有空间重叠
2. 确保 jingning 确实在 county 内部
3. 检查坐标系是否一致（脚本会自动处理）

## 📊 输出说明

脚本运行后会输出：

```
✅ 蒙版图层创建完成！
   输出组: mask_layers
   图层:
     • jingning_mask (透明度 50%)
     • county_excluding_jingning_mask (透明度 10%)

  📊 区域统计:
     • 内部区域(jingning): 123.45 km² | 白色蒙版 50%透明
     • 外部区域(扣除后): 456.78 km² | 白色蒙版 10%透明
```

## 🎬 完整工作流程

1. **准备数据**：加载 jingning 和 county 图层
2. **运行脚本**：生成蒙版图层
3. **调整顺序**：确保底图在蒙版下方
4. **微调效果**：调整透明度和颜色
5. **导出地图**：项目 → 布局管理器 → 导出

## 📝 技术说明

- 使用 QGIS 空间差集算法 (`native:difference`)
- 自动处理坐标系不一致问题
- 自动修复几何错误
- 输出为内存图层（快速，不占磁盘空间）

