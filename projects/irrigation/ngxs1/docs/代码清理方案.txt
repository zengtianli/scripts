================================================================================
                         代码清理方案
================================================================================

【目标】

    删除旧逻辑残留，让 dryland_models.py 更干净


【要删除的代码】

    1. calculate_water_requirements() 方法（第483-536行）
       - 老逻辑残留，调用 get_daily_water() 没传月份
       - 实际计算在 calculate_daily_water() 里做了
       - 这个方法现在没用
    
    2. _print_debug() 方法（第478-481行）
       - 没被使用
    
    3. crop_water_data 字段（第471行）
       - 老逻辑用的，现在不需要
    
    4. AreaLimits 类（第205-231行）
       - 面积限制验证，业务上不需要
    
    5. validate_areas() 方法（第398-435行）
       - 同上，不需要
    
    6. add_area_limits() 方法
       - 配套删除
    
    7. load_area_limits_from_file() 方法
       - 配套删除


【保留的核心逻辑】

    ┌─────────────────────────────────────────────────────────────┐
    │                                                             │
    │  Crop 类                                                    │
    │    - 存储月度定额                                            │
    │    - get_daily_water(保证率, 月份) → 日需水量               │
    │    - calculate_water_volume(面积, 保证率, 月份) → 总需水量   │
    │                                                             │
    │  CropRegistry 类                                            │
    │    - 管理作物列表                                            │
    │    - load_from_file() 加载月度定额数据                       │
    │                                                             │
    │  CropArea 类                                                │
    │    - 存储区域的作物种植面积                                  │
    │                                                             │
    │  AreaManager 类                                             │
    │    - 管理各区域的作物面积                                    │
    │    - load_crop_areas_from_file() 加载面积数据               │
    │                                                             │
    │  WaterRequirementCalculator 类                              │
    │    - calculate_daily_water() 计算某天某区域的需水量          │
    │    - apply_to_irrigation_areas() 应用到灌区                 │
    │                                                             │
    │  CropSimulator 类                                           │
    │    - 模拟器入口                                              │
    │    - initialize() 初始化                                    │
    │    - run() 运行计算                                         │
    │                                                             │
    └─────────────────────────────────────────────────────────────┘


【清理后代码行数】

    清理前：~710 行
    清理后：~450 行（预估减少 260 行）


【清理后的类结构】

    Crop                    作物（月度定额）
    CropRegistry            作物注册表
    CropArea                区域作物面积
    AreaManager             区域管理器（简化版）
    WaterRequirementCalculator  需水计算器（简化版）
    CropSimulator           模拟器入口


================================================================================
确认后我来操作
================================================================================
